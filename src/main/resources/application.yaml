server:
  port: 8081
  servlet:
    context-path: /api/v1

spring:
  application:
    name: food-court-microservice-foodcourt

  datasource:
    url: jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:6543/postgres?sslmode=require&preferQueryMode=simple
    username: postgres.fgemaxsxbefxrctlmpjj
    password: ${DB_FOODCOURT_FOODCOURT_PASSWORD}
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 2
      minimum-idle: 2
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 20000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  cloud:
    openfeign:
      circuitbreaker:
        enabled: true

jwt:
  secret: ${JWT_SECRET_KEY}
  validity: 3600000

user-service:
  url: http://localhost:8080/api/v1

message-service:
  url: http://localhost:8082/api/v1

traceability-service:
  url: http://localhost:8083/api/v1

feign:
  circuitbreaker:
    enabled: true